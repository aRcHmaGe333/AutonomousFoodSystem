Run the pod simulator

Usage:
  node simulator.js example_input.json

Example outputs will show kg_per_week, manual_person_hours_per_week, and pods_needed.

Tune inputs to match pilot measurements and iterate.